---
import DocLayout from '../../../layouts/DocLayout.astro';
---
<DocLayout
  title="How to debug a tricky bug with context_packet · agentmako"
  description="A walkthrough recipe — debug a generic 'hydration error' by starting from context_packet, narrowing with file_preflight, reef_inspect, and freshness, and pinning the actual cause."
  canonicalPath="/docs/recipes/debugging-with-context.html"
  ogImage="/assets/og.png"
  ogImageAlt="agentmako — debugging recipe."
  ogType="article"
  breadcrumbs={[{"name": "Home", "href": "/"}, {"name": "Docs", "href": "/docs/"}, {"name": "Recipes", "href": "/docs/recipes/"}, {"name": "Debug with context", "href": "/docs/recipes/debugging-with-context.html"}]}
>
<header class="doc-page-header">
  <span class="eyebrow">Recipe &middot; Debugging</span>
    <h1 style="margin-top:1rem;">How to <em>debug a tricky bug</em> with <code>context_packet</code></h1>
    <p class="doc-hero__sub">
      <strong>TL;DR:</strong> Hydration errors, "this only fails in
      production", "intermittent role check failure" — vague bugs eat
      hours when the agent grep-loops. Start from
      <code>context_packet</code>, branch into <code>file_preflight</code>,
      <code>reef_inspect</code>, and <code>trace_error</code>, then verify
      with <code>verification_state</code> before editing. Tools used:
      <code>context_packet</code>, <code>reef_scout</code>,
      <code>file_preflight</code>, <code>reef_inspect</code>,
      <code>trace_error</code>, <code>live_text_search</code>,
      <code>verification_state</code>.
    </p>
</header>
<section class="section">
  <div class="container">
    <article class="doc-body" style="max-width: 78ch;">

      <section>
        <h2>The bug</h2>
        <p>Sentry shows: <em>"Hydration failed because the initial UI
        does not match what was rendered on the server"</em>, frequency
        ~30 events/hour, mostly on the
        <code>/dashboard/manager/[districtId]</code> route. You have no
        idea where to start. The agent has even less.</p>
      </section>

      <section>
        <h2>Step 1 — <code>context_packet</code> with the error verbatim</h2>
        <pre><code>&lcub;
  "tool": "context_packet",
  "args": &lcub;
    "request": "Hydration failed on /dashboard/manager/[districtId]",
    "includeInstructions": true,
    "includeRisks": true,
    "includeLiveHints": true,
    "freshnessPolicy": "prefer_fresh",
    "budgetTokens": 4000
  &rcub;
&rcub;</code></pre>
        <p>The packet returns:</p>
        <ul>
          <li><strong>primaryContext</strong>: the layout file
          <code>app/dashboard/manager/[districtId]/layout.tsx</code>, the
          loading state, and the data fetcher.</li>
          <li><strong>activeFindings</strong>: a Reef finding from a
          prior session — "client-only date formatting in
          <code>DistrictHeader</code> uses Date.now() inline".</li>
          <li><strong>risks</strong>: "this layout reads from
          <code>manager_district</code>; RLS-sensitive."</li>
          <li><strong>recommendedHarnessPattern</strong>: "read primary
          → call <code>file_preflight</code> on layout.tsx → check
          freshness."</li>
        </ul>
        <p>The activeFindings line is gold — someone already looked at
        this file and noted a likely client/server mismatch culprit. Skip
        ahead to verifying it instead of re-deriving from scratch.</p>
      </section>

      <section>
        <h2>Step 2 — run <code>file_preflight</code> on the likely file</h2>
        <p>The Reef finding might be from three weeks ago and already
        fixed. Ask for the file's pre-edit packet:</p>
        <pre><code>&lcub;
  "tool": "file_preflight",
  "args": &lcub;
    "filePath": "app/dashboard/manager/[districtId]/layout.tsx",
    "limit": 50
  &rcub;
&rcub;</code></pre>
        <p>Returns durable findings, recent diagnostic runs that covered
        the file, stale diagnostic flags, conventions, and ack history.
        If it reports a stale source, let the daemon catch up or run
        <code>diagnostic_refresh</code> for that source before trusting
        the finding.</p>
      </section>

      <section>
        <h2>Step 3 — trace_error if you need broader context</h2>
        <p>If <code>context_packet</code> didn't surface the right file:</p>
        <pre><code>&lcub;
  "tool": "trace_error",
  "args": &lcub;
    "term": "Hydration failed",
    "limit": 30
  &rcub;
&rcub;</code></pre>
        <p>Returns throw sites, catch handlers, and PL/pgSQL bodies that
        match. For a runtime error this usually surfaces the React/Next
        boundary that's emitting it (and any custom error handlers in
        your app).</p>
      </section>

      <section>
        <h2>Step 4 — live_text_search after edits</h2>
        <p>You make a fix in <code>DistrictHeader</code> — wrap the
        time-sensitive render in a <code>useEffect</code> so it only
        runs on the client. Now you want to confirm there are no other
        instances of the same pattern. Indexed
        <code>cross_search</code> would be stale (you just edited).
        Use <code>live_text_search</code>:</p>
        <pre><code>&lcub;
  "tool": "live_text_search",
  "args": &lcub;
    "query": "new Date(",
    "pathGlob": "app/**/*.tsx",
    "fixedStrings": true,
    "maxMatches": 100
  &rcub;
&rcub;</code></pre>
        <p>Now you have a fresh ground-truth list of every
        <code>new Date(</code> in your app components. Audit them, fix
        any other inline-date-during-render cases.</p>
      </section>

      <section>
        <h2>Step 5 — verify and mine the pattern</h2>
        <p>After the fix, call <code>verification_state</code> for the
        changed files. If this was a repeatable structural bug, use
        <code>extract_rule_template</code> on the fix commit to draft a
        YAML rule, then validate it with <code>rule_pack_validate</code>.
        Ack findings only when they are reviewed and intentional.</p>
      </section>

      <section>
        <h2>Why this is faster than grep loops</h2>
        <p>The agent went from "vague bug report" to "fix shipped" in
        roughly 5 tool calls and ~2,000 tokens of context. The vanilla
        path would have looked like 12 grep calls, 6 file reads, 8,000+
        tokens, and the agent missing the prior Reef finding entirely.</p>
        <p>The Reef finding from a prior session is the key win. Every
        bug you debug becomes durable knowledge — the next person (or
        agent) gets a head start.</p>
      </section>

      <section>
        <h2>Tools used in this recipe</h2>
        <ul>
          <li><code>context_packet</code> — vague request → ranked context.</li>
          <li><code>reef_scout</code> — broader scout packet of facts/findings/rules.</li>
          <li><code>file_preflight</code> — pre-edit findings, diagnostic freshness, conventions, and acks.</li>
          <li><code>reef_inspect</code> — full evidence trail for one file/subject.</li>
          <li><code>trace_error</code> — find throw/catch sites for an error term.</li>
          <li><code>live_text_search</code> — exact current text on disk.</li>
          <li><code>verification_state</code> — confirms current diagnostics cover changed files.</li>
          <li><code>extract_rule_template</code> — propose a reusable YAML rule from a fix diff.</li>
          <li><code>lint_files</code> — Mako rule-pack diagnostics.</li>
        </ul>
      </section>

      <section id="related">
        <h2>Related</h2>
        <ul>
          <li><a href="../concepts/freshness.html">How agentmako tracks freshness</a></li>
          <li><a href="../concepts/context-packets.html">What is a context packet?</a></li>
          <li><a href="./auth-flow-tracing.html">How to trace an auth flow</a></li>
          <li><a href="../faq.html#stale">FAQ: why is agentmako returning stale results?</a></li>
        </ul>
      </section>

    </article>
  </div>
</section>
</DocLayout>
